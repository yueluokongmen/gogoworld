/**
 * 感知层模板（故意留空给你自己写）
 * 你后续可以在这里把 Mineflayer 的信息整理成 world state。
 */

function collectWorldState(_bot) {
  return {
    // TODO: 例如 position / inventory / nearbyBlocks / nearbyEntities
    // 先返回一个最小结构，避免调用方报错。
  };
}

module.exports = { collectWorldState };

